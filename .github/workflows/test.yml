name: Cron Hello World

on:
  schedule:
    - cron: '* * * * *' # Executa a cada hora, no início da hora

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello, World!"
